package java8features;

public abstract class Instrument {
    protected abstract void play();
}

// So, abstract classes must have at least package-private
// visibility (the default) or higher public


// So, abstract method must have at least package-private
// visibility (the default) or higher (protected or public)